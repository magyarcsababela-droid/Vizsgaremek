@inject ComputerpartsFrontendBlazor.Services.AuthService AuthService
@inject Microsoft.AspNetCore.Components.NavigationManager Navigation
@inject IJSRuntime JS
@rendermode InteractiveServer
@inherits LayoutComponentBase
<nav class="navbar navbar-expand-xl navbar-dark fixed-top shadow-sm" style="border-radius: 10px; border: 1px solid #3498db; margin: 1%;">
    <div class="container-fluid">
        <NavLink href="" class="logo nav-link">ComputerParts.com</NavLink>

        @* <a href="" class="logo">ComputerParts.com</a> *@
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse d-xl-flex flex-grow-0 gap-3" id="mainNavbar">
            <div class="d-none d-xl-flex gap-2">
                <a href="/" class="navbutton">Keződlap</a>
                <a href="parts" class="navbutton">Alkatrészek</a>
                <a href="configurator" class="navbutton">PC összerakó</a>
                <a href="deals" class="navbutton">Ajánlatok</a>
            </div>
            <!-- Auth test panel -->
            @* <section style="margin:1rem 0;">
                <h4>Auth teszt</h4>
                <div>
                    @if (AuthService?.IsAuthenticated == true)
                    {
                        <div style="color:lightgreen;">Bejelentkezve: @(string.IsNullOrEmpty(AuthService.Username) ? "(nincs felhasználónév)" : AuthService.Username)</div>
                        <div style="font-size:0.9rem; color:#ccc">Token: @ShortToken(AuthService.Token)</div>
                    }
                    else
                    {
                        <div style="color:salmon;">Nem vagy bejelentkezve</div>
                    }
                    <div style="margin-top:.5rem; display:flex; gap:.5rem; align-items:center;">
                        <button class="btn btn-sm btn-primary" @onclick="CheckAuthAsync">Frissíts állapot</button>
                        @if (AuthService?.IsAuthenticated == true)
                        {
                            <button class="btn btn-sm btn-danger" @onclick="LogoutAsync">Kijelentkezés</button>
                        }
                    </div>
                </div>
            </section> *@
            <div class="d-none d-xl-flex gap-2 align-items-center">
                @if (AuthService?.IsAuthenticated == true)
                {
                    <div class="dropdown">
                        <a class="icon-button d-flex align-items-center" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="Images/profile_user_avatar_people_icon_219228.png" alt="User" style="border-radius: 50%; width: 30px; height: 30px;" />
                        </a>
                        <div class="dropdown-menu">
                            <div style="color:lightgreen; font-weight: 10; font-size: 20px; padding: 10px 10px 0px 10px;">@(string.IsNullOrEmpty(AuthService.Username) ? "(nincs felhasználónév)" : AuthService.Username)</div>
                            @if (!string.IsNullOrEmpty(AuthService.Email))
                            {
                                <div style="color:#ccc; font-size:0.85rem; padding: 0 10px 10px 10px;">@AuthService.Email</div>
                            }
                            
                            @if (AuthService?.IsAuthenticated == true)
                            {
                                @if (string.Equals(AuthService.Role, "Admin", System.StringComparison.OrdinalIgnoreCase))
                                {
                                    <NavLink style="margin: 1%" class="dropdown-item btn btn-sm btn-danger" href="/admin">Admin oldal</NavLink>
                                }
                                <button style="margin: 1%" class="dropdown-item btn btn-sm btn-danger" @onclick="OpenProfileModal">Profil megtekintése</button>
                                <button style="margin: 1%" class="dropdown-item btn btn-sm btn-danger" @onclick="LogoutAsync">Kijelentkezés</button>
                            }
                        </div>
                    </div>
                }
                else
                {
                    
                    <div class="dropdown">
                        <a class="icon-button d-flex align-items-center" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                            <img src="Images/profile_user_avatar_people_icon_219228.png" alt="User" style="border-radius: 50%; width: 30px; height: 30px;" />
                        </a>
                        <div class="dropdown-menu" style="padding: 10px; margin: 1%; width: 100px">
                            <p style="color: white; margin: 1%">Jelentkezz be, vagy regisztrálj.</p>
                            <a href="register" class="btn dropdown-item" style="margin: 1%; text-align: center">Regisztráció</a>
                            <a href="login" class="btn dropdown-item" style="margin: 1%; text-align: center">Bejelentkezés</a>
                        </div>
                    </div>
                }
            </div>
            <div class="d-xl-none pt-3 d-grid gap-2">
                <a href="/" class="navbutton">Keződlap</a>
                <a href="parts" class="navbutton">Alkatrészek</a>
                <a href="configurator" class="navbutton">PC összerakó</a>
                <a href="deals" class="navbutton">Ajánlatok</a>
                <div class="mobile-icon-row d-flex gap-2 align-items-center">
                    @if (AuthService?.IsAuthenticated == true)
                    {
                        <div class="dropdown">
                            <a class="icon-button d-flex align-items-center" id="dropdownMenuButtonMobile" data-bs-toggle="dropdown" aria-expanded="false">
                                <img src="Images/profile_user_avatar_people_icon_219228.png" alt="User" style="border-radius: 50%; width: 30px; height: 30px;" />
                            </a>
                            <div class="dropdown-menu">
                                <div style="color:lightgreen; font-weight: 10; font-size: 20px; padding: 10px 10px 0px 10px;">@(string.IsNullOrEmpty(AuthService.Username) ? "(nincs felhasználónév)" : AuthService.Username)</div>
                                @if (!string.IsNullOrEmpty(AuthService.Email))
                                {
                                    <div style="color:#ccc; font-size:0.85rem; padding: 0 10px 10px 10px;">@AuthService.Email</div>
                                }

                                @if (string.Equals(AuthService.Role, "Admin", System.StringComparison.OrdinalIgnoreCase))
                                {
                                    <NavLink style="margin: 1%" class="dropdown-item btn btn-sm btn-danger" href="/admin">Admin oldal</NavLink>
                                }
                                <button style="margin: 1%" class="dropdown-item btn btn-sm btn-danger" @onclick="OpenProfileModal">Profil megtekintése</button>
                                <button style="margin: 1%" class="dropdown-item btn btn-sm btn-danger" @onclick="LogoutAsync">Kijelentkezés</button>
                            </div>
                        </div>
                    }
                    else
                    {
                    
                        <div class="dropdown">
                            <a class="icon-button d-flex align-items-center" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
                                <img src="Images/profile_user_avatar_people_icon_219228.png" alt="User" style="border-radius: 50%; width: 30px; height: 30px;" />
                            </a>
                            <div class="dropdown-menu" style="padding: 10px; margin: 1%; width: 100px">
                                <p style="color: white; margin: 1%">Jelentkezz be, vagy regisztrálj.</p>
                                <a href="register" class="btn dropdown-item" style="margin: 1%; text-align: center">Regisztráció</a>
                                <a href="login" class="btn dropdown-item" style="margin: 1%; text-align: center">Bejelentkezés</a>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</nav>

<ProfileModal Show="@showProfile" ShowChanged="EventCallback.Factory.Create<bool>(this, OnProfileShowChanged)" OpenBuildRequested="EventCallback.Factory.Create<int>(this, OnOpenBuildRequested)" />

@code {
    private bool showProfileDropdown = false;

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        // Feliratkozás a hitelesítési állapot változásaira, hogy a navigáció frissüljön a bejelentkezés/kijelentkezéskor
        try
        {
            AuthService.AuthStateChanged += OnAuthStateChanged;
        }
        catch
        {
            // Egyszerűen figyelmen kívül hagyható, ha a szolgáltatás még nem áll készen - a "StateHasChanged" meghívásával biztosítjuk, hogy amint elérhető lesz, frissül a komponens
        }

        // Meg kell bizonyosodni, hogy a service a "localStorage"-ot olyan hamar olvassa, amennyire csak lehet, hogy a komponensek időben, a helyes státuszban jelenjenek meg
        try
        {
            await AuthService.InitializeAsync();
        }
        catch { }
        StateHasChanged();
    }

    private void OnAuthStateChanged()
    {
        InvokeAsync(StateHasChanged);
    }

    private void ToggleProfileDropdown()
    {
        showProfileDropdown = !showProfileDropdown;
    }

    private async System.Threading.Tasks.Task OnLogoutClicked()
    {
        if (AuthService != null)
        {
            await AuthService.LogoutAsync();
        }

        Navigation.NavigateTo("/", true);
    }

    // "OnAfterRender" üresen tartása - az initializáció végbe megy az "OnInitializedAsync metóduson

    public void Dispose()
    {
        try
        {
            AuthService.AuthStateChanged -= OnAuthStateChanged;
        }
        catch { }
    }

    private string ShortToken(string? token)
    {
        if (string.IsNullOrEmpty(token)) return "(nincs token)";
        return token.Length > 40 ? token.Substring(0, 20) + "..." + token.Substring(token.Length - 16) : token;
    }

    private async Task CheckAuthAsync()
    {
        if (AuthService != null)
        {
            await AuthService.InitializeAsync();
        }
        StateHasChanged();
    }

    private async Task LogoutAsync()
    {
        if (AuthService != null)
        {
            await AuthService.LogoutAsync();
        }
        // Helyi állapot/felület frissítése
        await CheckAuthAsync();
        try { await JS.InvokeVoidAsync("setPendingToast", "Kijelentkezés sikeres.", "success", 4000, "bottom-center"); } catch { }
        try { await JS.InvokeVoidAsync("showToast", "Kijelentkezés sikeres.", "success", 4000, "bottom-center"); } catch { }
        StateHasChanged();
    }

    private bool showProfile = false;

    private void OpenProfileModal()
    {
        showProfile = true;
    }

    private async Task OnProfileShowChanged(bool val)
    {
        showProfile = val;
        await InvokeAsync(StateHasChanged);
    }

    private async Task OnOpenBuildRequested(int id)
    {
        // Close the modal in this parent and then force a full navigation via JS to ensure immediate load.
        showProfile = false;
        await InvokeAsync(StateHasChanged);
        try { await Task.Delay(80); } catch { }
        var url = Navigation.ToAbsoluteUri($"/configurator?buildId={id}").ToString();
        try
        {
            await JS.InvokeVoidAsync("open", url, "_self");
        }
        catch
        {
            try { Navigation.NavigateTo($"/configurator?buildId={id}", true); } catch { }
        }
    }

    private void GoToAdmin()
    {
        try
        {
            Navigation.NavigateTo("/admin");
        }
        catch { }
    }
}
